{
  "name": "localserver",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://yakov29.github.io/fruits/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build",
    "server": "npx json-server fruits.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "gh-pages": "^6.1.1",
    "json-server": "^1.0.0-beta.1"
  }
}
